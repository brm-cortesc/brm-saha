buscarBtn  = $('a.icon-search')
carritoBtn = $('a.icon-cart')
userBtn    = $('a.icon-user')

jQuery(document).ready ->

	### user menu clicks ###
	buscarBtn
		.click (e) ->
			e.preventDefault()
		
			return

	return